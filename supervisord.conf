[supervisord]
nodaemon=true
user=root
logfile=/var/log/supervisor/supervisord.log
pidfile=/var/run/supervisord.pid

[program:php]
command=%(ENV_SUPERVISOR_PHP_COMMAND)s
user=%(ENV_SUPERVISOR_PHP_USER)s
environment=LARAVEL="1"
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0

#[program:ssh_sridesbds09]
#command=/bin/sh -lc 'cd /root/.ssh && set -- *.pem; pem="$1"; user="${pem##*/}"; user="${user%%.pem}"; exec ssh -L 3306:sridesbds09.c1v8vajd21xm.us-east-1.rds.amazonaws.com:3306 -i "$pem" -v "$user@10.54.24.10" -N'
#user=root
#environment=LARAVEL="1"
#stdout_logfile=/dev/stdout
#stdout_logfile_maxbytes=0
#stderr_logfile=/dev/stderr
#stderr_logfile_maxbytes=0
